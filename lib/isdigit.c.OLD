#include <xinu.h>

/* isdigit.c - isdigit */

/*------------------------------------------------------------------------
 * Return 1 if the argument is a digit, or 0 otherwise
 *------------------------------------------------------------------------
 */
int32	isdigit(
	  int32	c
	)
{
	return (c >= '0' && c <= '9');
}
